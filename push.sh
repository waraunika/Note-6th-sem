#!/bin/bash

message1="Started on Ch-1 for Communication System"
message2="Completed Chapter 1, and created file for chapter 2 for CS, based on PYQs"

git add .
git commit -m "$message1" -m "$message2"
git push -u origin main
